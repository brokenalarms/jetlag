.DS_Store
.claude/
__pycache__
.vscode
.coverage
.vite
.ralph

# Python venv (recreated locally by ensure-venv.sh; app bundle installs deps fresh at build time)
scripts/.venv/

# Performance baseline is machine-specific (record with: pytest --perf-baseline)
scripts/tests/perf_baseline.json

# Environment files with secrets
.env.local
.env

# Vendored tool binaries (downloaded/built locally per developer — see scripts/tools/README)
# scripts/tools/ffprobe
# scripts/tools/gyroflow
# scripts/tools/jetlag-metadata

# Xcode generated
*.xcodeproj
xcuserdata/

# Swift Package Manager build artifacts
.build/

