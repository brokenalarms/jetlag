name: Jetlag
options:
  bundleIdPrefix: com.daniellawrence
  deploymentTarget:
    macOS: "14.0"
  xcodeVersion: "16.0"
  minimumXcodeGenVersion: "2.38.0"
packages:
  Yams:
    url: https://github.com/jpsim/Yams
    from: "5.0.0"
targets:
  Jetlag:
    type: application
    platform: macOS
    sources:
      - path: Sources
        excludes:
          - "Tools"
    dependencies:
      - package: Yams
      - sdk: MapKit.framework
      - sdk: CoreLocation.framework
      - sdk: Quartz.framework
    scheme:
      testTargets:
        - JetlagTests
      environmentVariables:
        # Debug builds only — LicenseStore ignores this in Release.
        JETLAG_PRO_UNLOCKED: "1"
    settings:
      base:
        # Empty in the main checkout and the Xcode IDE, so those builds carry the
        # installed app's identity as before. Builds under .ralph/ pass their own
        # values on the xcodebuild command line — see the pre-build guard below.
        JETLAG_BUNDLE_SUFFIX: ""
        JETLAG_PRODUCT_SUFFIX: ""
        PRODUCT_BUNDLE_IDENTIFIER: com.daniellawrence.Jetlag$(JETLAG_BUNDLE_SUFFIX)
        PRODUCT_NAME: Jetlag$(JETLAG_PRODUCT_SUFFIX)
        INFOPLIST_KEY_CFBundleDisplayName: Jetlag$(JETLAG_PRODUCT_SUFFIX)
        # The Swift module name defaults to the product name, which would make a
        # suffixed build's module `Jetlag_Dev` and break `@testable import Jetlag`.
        # It is not part of the app's identity on disk, so it stays fixed.
        PRODUCT_MODULE_NAME: Jetlag
        MARKETING_VERSION: "0.1.0"
        CURRENT_PROJECT_VERSION: "1"
        SWIFT_VERSION: "5.9"
        MACOSX_DEPLOYMENT_TARGET: "14.0"
        GENERATE_INFOPLIST_FILE: YES
        # Suppress xcodegen's Info.plist auto-detection, which otherwise picks up
        # the jetlag-metadata dSYM build artifact under Sources/Tools/.build.
        INFOPLIST_FILE: ""
        INFOPLIST_KEY_LSApplicationCategoryType: public.app-category.video
        INFOPLIST_KEY_NSHumanReadableCopyright: "© 2026 Daniel Lawrence"
        CODE_SIGN_ENTITLEMENTS: Sources/App.entitlements
    preBuildScripts:
      - name: Guard Worktree Bundle Identity
        basedOnDependencyAnalysis: false
        script: |
          "${SRCROOT}/BuildScripts/guard-worktree-identity.sh"
    postBuildScripts:
      - name: Bundle scripts
        script: |
          set -e
          DEST="${BUILT_PRODUCTS_DIR}/${PRODUCT_NAME}.app/Contents/Resources/scripts"
          rm -rf "$DEST"
          cp -r "${SRCROOT}/../scripts/" "$DEST"
          # A checkout's .venv holds absolute paths into the checkout — the bundle
          # imports from site-packages below instead. Prune at any depth: a venv
          # created from the wrong working directory nests one level down.
          find "$DEST" -name .venv -type d -prune -exec rm -rf {} +
          find "$DEST" -name __pycache__ -type d -prune -exec rm -rf {} +
          # Dev tooling (pytest, pyrefly) is never vendored — drop the manifest so
          # nothing in the bundle can install from it.
          rm -f "$DEST/requirements-dev.txt"
          # Vendor dependencies with the same interpreter the app runs them on
          # (lib/ensure-venv.sh resolves to /usr/bin/python3) — PyYAML ships a C
          # extension, so wheels built for another version fail to import.
          # pip resolution takes ~30-60s per build; cache the installed tree in
          # DerivedData keyed by the requirements hash and copy it in instead.
          rm -rf "$DEST/site-packages"
          REQ_HASH=$(shasum "$DEST/requirements.txt" | awk '{print $1}')
          PIP_CACHE="${BUILT_PRODUCTS_DIR}/jetlag-site-packages-cache"
          if [ ! -f "$PIP_CACHE/.stamp" ] || [ "$(cat "$PIP_CACHE/.stamp")" != "$REQ_HASH" ]; then
            rm -rf "$PIP_CACHE"
            /usr/bin/python3 -m pip install --quiet --target "$PIP_CACHE" -r "$DEST/requirements.txt"
            echo "$REQ_HASH" > "$PIP_CACHE/.stamp"
          fi
          cp -R "$PIP_CACHE" "$DEST/site-packages"
          rm -f "$DEST/site-packages/.stamp"
          for dev_pkg in pytest _pytest pyrefly; do
            if [ -e "$DEST/site-packages/$dev_pkg" ]; then
              echo "error: dev tooling '$dev_pkg' vendored into the bundle — move it from scripts/requirements.txt to scripts/requirements-dev.txt" >&2
              exit 1
            fi
          done

          # Build jetlag-metadata Swift CLI and install into bundle
          METADATA_PKG="${SRCROOT}/Sources/Tools/jetlag-metadata"
          if [ -d "$METADATA_PKG" ]; then
            swift build -c release --package-path "$METADATA_PKG" 2>&1
            cp "$METADATA_PKG/.build/release/jetlag-metadata" "$DEST/tools/jetlag-metadata"
          fi

          # Verify vendored tools are present (copied by cp -r above)
          for tool in exiftool tag ffprobe jetlag-metadata; do
            if [ ! -f "$DEST/tools/$tool" ]; then
              echo "error: scripts/tools/$tool missing — run scripts/tools/download-${tool}.sh or scripts/tools/build-jetlag-metadata.sh or see scripts/tools/README" >&2
              exit 1
            fi
          done
  JetlagTests:
    type: bundle.unit-test
    platform: macOS
    sources:
      - Tests
    dependencies:
      - target: Jetlag
      # PipelineSchemaContractTests reads scripts/pipeline-schema.yaml directly —
      # the schema is the fixture source, not a copy kept in the test bundle.
      - package: Yams
    settings:
      base:
        JETLAG_BUNDLE_SUFFIX: ""
        JETLAG_PRODUCT_SUFFIX: ""
        PRODUCT_BUNDLE_IDENTIFIER: com.daniellawrence.JetlagTests$(JETLAG_BUNDLE_SUFFIX)
        # xcodegen writes the unsuffixed product name into TEST_HOST; a suffixed
        # build's host app is named for its own identity.
        TEST_HOST: $(BUILT_PRODUCTS_DIR)/Jetlag$(JETLAG_PRODUCT_SUFFIX).app/Contents/MacOS/Jetlag$(JETLAG_PRODUCT_SUFFIX)
        SWIFT_VERSION: "5.9"
        GENERATE_INFOPLIST_FILE: YES
